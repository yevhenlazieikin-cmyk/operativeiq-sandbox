/**
 * AI Agent constants
 */
export const AI_AGENT_COOKIE_NAME = 'AIAgentCookie';

/**
 * Delay in milliseconds for AI agent script initialization
 */
export const AI_AGENT_SCRIPT_INIT_DELAY = 100;

/**
 * Delay in milliseconds for AI agent context update
 */
export const AI_AGENT_CONTEXT_UPDATE_DELAY = 100;

/**
 * Delay in milliseconds for preselection of menu items
 */
export const AI_AGENT_PRESELECT_DELAY = 100;

/**
 * Delay in milliseconds for deferring token execution to next event loop tick
 */
export const AI_AGENT_TOKEN_DEFER_DELAY = 500;
